[pytest]
testpaths = tests
asyncio_mode = auto
# The backend is laid out as flat modules imported by name (main, auth, …), the
# way uvicorn loads it, so the tests import them the same way.
pythonpath = .
filterwarnings =
    error::DeprecationWarning:influx_store
    error::DeprecationWarning:tibber_pulse
